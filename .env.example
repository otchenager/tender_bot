EMAIL_FROM=your_gmail@gmail.com
EMAIL_TO=customer@gmail.com
EMAIL_PASSWORD=your_app_password
ANTHROPIC_API_KEY=sk-ant-api03-...
ANTHROPIC_MODEL=claude-sonnet-4-6
DATABASE_URL=postgresql://user:password@localhost:5432/tender_bot

# Split-deployment (Railway side)
INGEST_API_KEY=tender_secret_key_2026

# Split-deployment (VPS side — also needs INGEST_API_KEY above)
RAILWAY_URL=https://your-app.railway.app
# Budget/region filtering (formerly MIN_BUDGET/MIN_BUDGET_SINGLE_SOURCE/REGIONS/
# VPS_KEYWORDS here) now lives in Railway's search_settings table, edited from
# the dashboard Settings tab — the VPS no longer filters, it only skips
# marketing-type placeholders and inactive/closed lots.
